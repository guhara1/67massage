name: GA4 일일 리포트 → 텔레그램

# 자동 발송(매일 9시) 일시중지 — GA4 서비스계정 뷰어 권한 완료 후 아래 schedule 주석을 풀면 됩니다.
on:
  # schedule:
  #   - cron: "0 0 * * *"   # 매일 한국시간 09:00(UTC 00:00)
  workflow_dispatch: {}   # 수동 실행 버튼만 유지

permissions:
  contents: read

jobs:
  report:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: 의존성 설치
        run: pip install --quiet google-analytics-data

      - name: 서비스계정 키 파일 생성
        run: |
          printf '%s' "$GA_SERVICE_ACCOUNT_JSON" > "$RUNNER_TEMP/ga.json"
          echo "ga.json 바이트수: $(wc -c < "$RUNNER_TEMP/ga.json")  (0이면 시크릿 비었음)"
          head -c 1 "$RUNNER_TEMP/ga.json" | grep -q '{' && echo "JSON 시작 '{' 확인 OK" || echo "⚠️ JSON이 '{'로 시작하지 않음 — 시크릿 값 확인 필요"
        env:
          GA_SERVICE_ACCOUNT_JSON: ${{ secrets.GA_SERVICE_ACCOUNT_JSON }}

      - name: 리포트 발송
        env:
          GOOGLE_APPLICATION_CREDENTIALS: ${{ runner.temp }}/ga.json
          GA4_PROPERTY_ID: ${{ secrets.GA4_PROPERTY_ID }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python3 _ga4_report.py
